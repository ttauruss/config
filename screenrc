defescape ``
escape ``

defscrollback 1024

startup_message off

hardstatus on
hardstatus alwayslastline
hardstatus string "%{.kW}%-w%{.bW}%n %t%{-}%+w %=%{..G} %{..Y} %Y/%m/%d %c"

# fix backspace issue
bindkey -d ^? stuff ^H
bindkey -d ^@ stuff ^H

screen -t '' 0 bash
select 0
